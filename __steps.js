/**
 * Basic Context Setu ---
 * 1. contex api : share auth state with other components (over the app).
 * 2. Create an UserContext object
 * 3. Create contex provider - with children
 * 4. set the contextprovider(UserContext) ion the index.js
 * 5. now to consume the context : export the AuthContext from userContext
 * 6. Now at Header or anyWhere else : use UseContext hook
 */


/**
 * Auth Integration  ---
 * 1. use GgetAuth from firebaseS
 * 2. create a function named createUser and return createUserWithEmailAndPassword
 */